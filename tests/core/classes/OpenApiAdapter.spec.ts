import {
    adapter,
    requestBodyStringSerializer,
    OpenApiAdapter,
    DEFAULT_PARAMETER_SERIALIZATION_SETTINGS, 
} from '@openapi-adapter/core'
import {TestApiSpecification} from '../../__mocks__/TestApi.specification'

describe('core/classes/OpenApiAdapter', () => { 

    it('test',()=>{})

})