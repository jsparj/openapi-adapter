{
  "name": "openapi-adapter",
  "private": true,
  "engines": {},
  "scripts": {
    "build": "rm -rf ./dist && tsc"
  },
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "files": [
    "dist"
  ]
}
